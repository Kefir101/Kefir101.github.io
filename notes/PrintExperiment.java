// //import javax.swing.*;
// //import java.util.Scanner;

// public class PrintExperiment{
//     public static void main(String[] args) {
//         String r1, r2, r3, r4;
//         // double x1, x2, y1, y2;
//         //r1 = JOptionPane.showInputDialog("What is the x value of the first point?");
//         // x1 = Double.parseDouble(r1);
//         // r2 = JOptionPane.showInputDialog("What is the x value of the second point?");
//         // x2 = Double.parseDouble(r2);
//         // r3 = JOptionPane.showInputDialog("What is the y value of the first point?");
//         // y1 = Double.parseDouble(r3);
//         // r4 = JOptionPane.showInputDialog("What is the y value of the second point?");
//         // y2 = Double.parseDouble(r4);
//         // double slope = (y2 - y1)/(x2 - x1);
//         // double yint = y1 - (x1*slope);
//         // JOptionPane.showMessageDialog(null, "The line y = " + slope + "x + " + yint +
//         // " passes through the points (" +x1 + ", " + y1 + ")" + "and (" +x2 + ", " + y2 + ")");
//         System.out.println("please printttt");
//     }
//     public void visitStringNode (Text string)
//     {
//         if (string.getText().equals("**text**")) {
//             string.setText("**new text**");
//         }
//     }
// }